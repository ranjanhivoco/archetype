/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['careerarchetypes.s3.ap-south-1.amazonaws.com'],
  },

  reactStrictMode: true,
};

export default nextConfig;

